import Foundation

/// Copy this file to Secrets.swift and fill in your values.
/// Secrets.swift is gitignored and will not be committed.
enum Secrets {
    static let supabaseURL = URL(string: "https://YOUR_PROJECT.supabase.co")!
    static let supabasePublishableKey = "YOUR_PUBLISHABLE_KEY"
}
